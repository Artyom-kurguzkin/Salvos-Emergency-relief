<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: black;
            margin: 0;
            padding: 0;
        }

        .container {
            text-align: center;
            padding: 20px;
        }

        .logo {
            max-width: 150px;
            margin: 20px auto;
        }

        .welcome {
            font-size: 24px;
            font-weight: bold;
            color: black;
        }

        .subtext {
            color: #666;
        }

        .input-box {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
        }

        .password {
            position: relative;
        }

        .show-password {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
        }

        .forgot-password {
            color: #666;
            text-decoration: underline;
            cursor: pointer;
        }

        .login-button {
            background-color: black;
            color: white;
            border: none;
            padding: 10px 20px;
            font-weight: bold;
            cursor: pointer;
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.4);
        }

        .modal-content {
            background-color: white;
            margin: 15% auto;
            padding: 20px;
            width: 60%;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        }

        .close {
            color: black;
            float: right;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <img class="logo" src=//"savlos logo"// alt="Company Logo">
        <div class="welcome">Welcome back!</div>
        <div class="subtext">Enter staff details</div>
        <input class="input-box" type="text" placeholder="Staff ID">
        <div class="password">
            <input class="input-box" type="password" id="password" placeholder="Password">
            <span class="show-password" onclick="showPassword()">Show</span>
        </div>
        <div class="forgot-password" onclick="openForgotPasswordModal()">Forgot Password?</div>
        <button class="login-button" onclick="login()">Login</button>
    </div>

    <div id="forgotPasswordModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeForgotPasswordModal()">&times;</span>
            <h2>Forgot Password</h2>
            <p>Please enter your registered email address to reset your password.</p>
            <input class="input-box" type="email" id="forgotEmail" placeholder="Email">
            <button class="login-button" onclick="sendPasswordResetEmail()">Reset Password</button>
        </div>
    </div>

    <script>
        function showPassword() {
            var passwordInput = document.getElementById('password');
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
            } else {
                passwordInput.type = "password";
            }
        }

        function openForgotPasswordModal() {
            var modal = document.getElementById('forgotPasswordModal');
            modal.style.display = 'block';
        }

        function closeForgotPasswordModal() {
            var modal = document.getElementById('forgotPasswordModal');
            modal.style.display = 'none';
        }

        function sendPasswordResetEmail() {
            var email = document.getElementById('forgotEmail').value;
            // Implement code to send a password reset email to the provided email address.
        }

        function login() {
            // Implement the login functionality and validation here.
            // If login is successful, redirect to the appropriate page.
            // If login fails, display an error message.
        }
    </script>
</body>
</html>
